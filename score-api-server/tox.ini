[tox]
envlist = pep8,unittests,venv
minversion = 1.9
skipsdist = True


[testenv]
setenv = VIRTUAL_ENV={envdir}
usedevelop = True
install_command = pip install -U {opts} {packages}
deps = -r{toxinidir}/requirements.txt
       -r{toxinidir}/test-requirements.txt
       -r{toxinidir}/dev-requirements.txt
whitelist_externals = bash


[testenv:pep8]
commands =
    flake8 score_api_server ../blueprints tests {posargs}

[testenv:unittests]
commands =
    nosetests tests/ --with-coverage  --cover-package=score_api_server

[testenv:venv]
commands = {posargs}

[flake8]
show-source = True
ignore = H306,H301,H405
exclude=.venv,.tox,dist,*egg,etc,build,.idea
filename=*.py
