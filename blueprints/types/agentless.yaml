node_types:

    # template for nodejs node
    agentless.nodes.NodeJSServer:
        derived_from: cloudify.nodes.WebServer
        interfaces:
            cloudify.interfaces.lifecycle:
                start:
                    implementation: fabric.fabric_plugin.tasks.run_task
                    inputs:
                        tasks_file:
                            default: scripts/install_nodejs.py
                        task_name:
                            default: install
                        task_properties:
                            default:
                             config: {}

    # template for mongo node
    agentless.nodes.MongoDatabase:
        derived_from: cloudify.nodes.DBMS
        interfaces:
            cloudify.interfaces.lifecycle:
                start:
                    implementation: fabric.fabric_plugin.tasks.run_task
                    inputs:
                        tasks_file:
                            default: scripts/install_mongo.py
                        task_name:
                            default: install
                        task_properties:
                            default:
                             config: {}

    # template for postgresql node
    agentless.nodes.PostgreSqlDatabase:
        derived_from: cloudify.nodes.DBMS
        interfaces:
            cloudify.interfaces.lifecycle:
                start:
                    implementation: fabric.fabric_plugin.tasks.run_task
                    inputs:
                        tasks_file:
                            default: scripts/install_postgresql.py
                        task_name:
                            default: install
                        task_properties:
                            default:
                             config: {}

    #template for nodejs application nodecellar
    agentless.nodes.nodecellar:
        derived_from: cloudify.nodes.ApplicationModule
        interfaces:
            cloudify.interfaces.lifecycle:
                start:
                    implementation: fabric.fabric_plugin.tasks.run_task
                    inputs:
                        tasks_file:
                            default: scripts/install_nodecellar.py
                        task_name:
                            default: install
                        task_properties:
                            default:
                             config: {}
