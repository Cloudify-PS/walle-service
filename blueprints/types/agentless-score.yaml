node_types:

  # template for gunicorn node
  agentless-score.nodes.GunicornServer:
    derived_from: cloudify.nodes.WebServer
    interfaces:
      cloudify.interfaces.lifecycle:
        start:
          implementation: fabric.fabric_plugin.tasks.run_task
          inputs:
            tasks_file:
              default: scripts/score/install_dependencies.py
            task_name:
              default: install
            task_properties:
              default:
               config: {}

  #template for gunicorn application score
  agentless-score.nodes.score:
    derived_from: cloudify.nodes.ApplicationModule
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: fabric.fabric_plugin.tasks.run_task
          inputs:
            tasks_file:
              default: scripts/score/install_score.py
            task_name:
              default: install
            task_properties:
              default:
               config: {}
        start:
          implementation: fabric.fabric_plugin.tasks.run_task
          inputs:
            tasks_file:
              default: scripts/score/configure.py
            task_name:
              default: configure
            task_properties:
              default:
               config: {}
